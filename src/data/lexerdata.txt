var const global typeof delete String Number Boolean Array and or not equals if else while until do for
print write readNumber readString readLine
null infinity true false
()[]{},:;
+ += - -= * *= / /= % %= = == === != !== > >= < <= && || ? . []
! ++ --
+-*/%=?&|<>!.
